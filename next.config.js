const path = require('path')

module.exports = {
    images: {
        domains: ['images.prismic.io'],
    },
    sass: true,
    modules: true,
    // sassOptions: {
    //     includePaths: [path.join(__dirname, 'styles')],
    // },
    trailingSlash: true,

    // i18n: {    
    //     locales: ['en-gb','en-us'],   
    //     defaultLocale: 'en-gb',
    // },
}